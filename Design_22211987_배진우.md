# <center>1. Major Share (전공 자산 선순환 네트워크)</center>
### <center>- Design -</center>

| Student No | 22211987 |
| :--- | :--- |
| Name | 배진우|
| E-mail | ajdjjaja292983474@gmail.com |


## [ Revision history ]
| Revision date | Version # | Description | Author |
| :--- | :--- | :--- | :--- |
| 2026/05/08 | 1.00 | First draft | 배진우 |

## = Contents =
1. [Introduction]
2. [Class diagram]
3. [Sequence diagram]
4. [State machine diagram]
5. [Implementation requirements]
6. [Glossary]
7. [References]


## 1. Introduction

### 1) Summary
대학생들은 매 학기 비싼 전공 서적과 학습 기기(태블릿, 공학용 계산기 등)를 필요로 한다. 하지만 한 학기 남짓 짧은 기간을 위해 매번 새 책이나 기기를 구매하는 것은 학생들에게 금전적으로 매우 큰 부담이다. 그렇다고 교내 익명 커뮤니티나 중고 거래 플랫폼을 이용하자니, 자신이 원하는 특정 전공 물품을 제때 찾기 힘들뿐더러 거래 상대방의 신뢰성을 보장받기도 어렵다. 특히 매매가 아닌 '대여'의 경우에는 반납 기한이 제대로 지켜지지 않거나 물품이 훼손되는 등 보이지 않는 문제들이 발생하기 쉬워 개인 간 거래가 활성화되기 힘든 구조다.이러한 대학생들의 경제적 부담을 줄이고, 같은 학교 학생들끼리 안전하고 효율적으로 전공 지식 자산을 순환할 수 있도록 도와주고 싶었다. 그리하여 대학교 전공 서적 및 학습 자산 공유 플랫폼 ‘Major-Share’를 개발하게 되었다. 이 프로그램을 만들어서 이루고자 하는 목표는 효율적인 자원 순환이다. 수요자는 필요한 기간만큼만 물품을 저렴하게 대여하거나 구매할 수 있고, 공급자는 방치된 자산을 공유하여 이익을 얻을 수 있다. 또 다른 목표는 신뢰 기반의 안전한 거래 환경 조성이다. 거래가 완료되면 상호 리뷰를 통해 매너 점수를 평가하고, 예약 시스템 및 대여 기간 연장 기능을 제공하여 분쟁을 최소화한다. 또한 관리자(Admin)가 악성 유저와 부적절한 게시물을 제재하여 플랫폼의 청결도를 유지한다.

### 2) Important Points of Design

* 역할 분리 및 무상태(Stateless) 제어:`User`, `Item`과 같은 데이터 중심의 엔티티(Entity) 클래스와 `AuthManager`, `Admin`과 같은 비즈니스 제어(Controller) 클래스의 역할을 명확히 구별한다. 제어 클래스는 동시성 문제를 방지하기 위해 속성을 가지지 않는 무상태로 설계한다.
* 객체 간 상태 동기화 및 캡슐화: 특정 오퍼레이션이 실행될 때 연관된 객체의 상태가 함께 변하도록(예: `Transaction` 승인 시 대상 `Item`의 상태 변경, `Review` 작성 시 `User`의 평점 갱신) 설계하되, 불필요한 정보는 외부로 노출되지 않도록 캡슐화를 유지해야 한다.


## 2.Class Diagram

<img width="13900" height="10600" alt="image" src="https://github.com/user-attachments/assets/978e7fdd-6a84-40b9-acd3-10f87892412a" />

### Class diagram Details

| Class Name | Explanation |
| :--- | :--- |
| **AuthManager** | 시스템의 인증 및 접근 권한을 총괄하는 인증 관리자(Controller) 클래스이다. 동시성 문제를 방지하기 위해 속성을 가지지 않는 무상태(Stateless)로 설계되었다.<br><br>**[Attributes]**<br>*(비즈니스 로직만 처리하는 무상태 제어 클래스이므로 고유 데이터 속성 없음)*<br><br>**[Operations]**<br>**<비즈니스 로직>**<br>· `+ registerUser(userId: String, password: String, name: String): User` : 사용자 등록(회원가입) 프로세스를 처리하고, 초기화된 순수 데이터 객체인 User 인스턴스를 생성하여 반환한다.<br>· `+ login(userId: String, password: String): boolean` : 입력된 자격 증명(ID/PW)을 바탕으로 사용자의 로그인 인증을 처리한다.<br>· `+ logout(userId: String): void` : 특정 사용자의 로그아웃 프로세스(세션 만료 등)를 처리한다.<br>· `+ isAuthenticated(userId: String): boolean` : 특정 사용자가 현재 인증된 상태(로그인 상태)인지 여부를 검증하여 반환한다. |
| **Admin** | 시스템 모니터링 및 제재를 총괄하는 관리자(Controller) 클래스이다. 동시성 문제를 방지하기 위해 속성을 가지지 않는 무상태(Stateless)로 설계되었다.<br><br>**[Attributes]**<br>*(무상태 제어 클래스이므로 고유 속성을 가지지 않음)*<br><br>**[Operations]**<br>**<비즈니스 로직>**<br>· `+ loadSystemData(): SystemDataDTO` : 관리자 화면에 필요한 전체 시스템 통계 및 목록을 반환한다.<br>· `+ viewUserTransactions(targetUser: User): List<Transaction>` : 타겟 유저의 과거 및 현재 거래 내역을 조회한다.<br>· `+ applySuspension(targetUser: User, days: int): void` : 타겟 유저에게 지정된 일수만큼의 계정 정지 처분을 내린다.<br>· `+ applyPermanentBan(targetUser: User): void` : 악성 타겟 유저를 영구 정지 처리한다.<br>· `+ deleteInappropriateItem(targetItem: Item): void` : 규정을 위반한 타겟 게시글을 강제 삭제한다. |
| **User** | 시스템의 기본 주체 엔티티 클래스이다. 공급자와 수요자를 모두 포함하며, 회원의 정보와 활동 평점을 관리한다.<br><br>**[Attributes]**<br>· `- userId: String` : 사용자의 고유 로그인 아이디<br>· `- password: String` : 암호화된 비밀번호<br>· `- name: String` : 사용자의 실제 이름 또는 닉네임<br>· `- role: String` : 시스템 권한 등급 (일반, 관리자 등)<br>· `- accountStatus: String` : 계정의 현재 상태 (활성, 정지 등)<br>· `- /mannerScore: float` : 받은 리뷰를 바탕으로 계산되는 매너 점수(도출 속성)<br><br>**[Operations]**<br>**<비즈니스 로직>**<br>· `+ User(userId: String, password: String, name: String)` : 초기 정보를 바탕으로 사용자 객체를 생성한다.<br>· `+ updatePassword(password: String): void` : 새로운 비밀번호로 변경한다.<br>· `+ updateName(name: String): void` : 사용자의 이름을 변경한다.<br>· `+ updateScore(rating: int): void` : 새로운 평점을 반영하여 매너 점수를 갱신한다.<br>· `+ getProfileResponse(): UserProfileDTO` : 화면 출력용 프로필 데이터(DTO)를 반환한다.<br>**<Getters (데이터 조회용)>**<br>· `+ getUserId(): String` : 아이디를 조회한다.<br>· `+ getPassword(): String` : 비밀번호를 조회한다.<br>· `+ getName(): String` : 이름을 조회한다.<br>· `+ getRole(): String` : 권한 등급을 조회한다.<br>· `+ getAccountStatus(): String` : 계정 상태를 조회한다.<br>· `+ getMannerScore(): float` : 매너 점수를 조회한다. |
| **Item** | 공유 및 거래의 대상이 되는 전공 서적 및 자산 엔티티 클래스이다.<br><br>**[Attributes]**<br>· `- itemId: Long` : 등록된 물품의 고유 식별 번호<br>· `- title: String` : 물품의 제목 또는 서명<br>· `- category: String` : 분류 카테고리 (전공, 교양 등)<br>· `- transactionType: String` : 거래 방식 (매매 또는 대여)<br>· `- status: String` : 현재 상태 (대여 가능, 대여 중 등)<br>· `- maxRentDays: int` : 최대 대여 가능 일수<br>· `- price: int` : 판매 금액 또는 일일 대여 요금<br>· `- owner: User` : 물품을 등록한 소유자 객체<br><br>**[Operations]**<br>**<비즈니스 로직>**<br>· `+ Item(title: String, category: String, transactionType: String, price: int, maxRentDays: int, owner: User)` : 입력된 정보로 새로운 물품 객체를 생성한다.<br>· `+ searchItem(title: String, category: String, status: String): List<Item>` : 조건에 맞는 물품 목록을 검색한다.<br>· `+ updateTitle(title: String): void` : 물품의 제목을 수정한다.<br>· `+ changePrice(price: int): void` : 물품의 가격을 변경한다.<br>· `+ changeStatus(newStatus: String): void` : 물품의 거래 상태를 변경한다.<br>· `+ getItemDetailResponse(): ItemDetailDTO` : 물품 상세 정보 데이터(DTO)를 반환한다.<br>**<Getters (데이터 조회용)>**<br>· `+ getItemId(): Long` : 물품 번호를 조회한다.<br>· `+ getTitle(): String` : 제목을 조회한다.<br>· `+ getCategory(): String` : 카테고리를 조회한다.<br>· `+ getTransactionType(): String` : 거래 방식을 조회한다.<br>· `+ getStatus(): String` : 현재 상태를 조회한다.<br>· `+ getMaxRentDays(): int` : 최대 대여 일수를 조회한다.<br>· `+ getPrice(): int` : 가격을 조회한다.<br>· `+ getOwner(): User` : 소유자 객체를 조회한다. |
| **Transaction** | 공급자와 수요자 사이에 성립된 거래 계약 정보를 저장하는 엔티티 클래스이다.<br><br>**[Attributes]**<br>· `- transactionId: Long` : 체결된 거래의 고유 식별 번호<br>· `- stage: String` : 현재 진행 단계 (승인 대기, 수령, 반납 등)<br>· `- transactionType: String` : 체결된 거래의 종류<br>· `- transactionDate: Date` : 거래가 승인되어 시작된 날짜<br>· `- returnDueDate: DateTime` : 물품 반납 마감 기한<br>· `- buyer: User` : 거래를 신청한 수요자 객체<br>· `- subjectItem: Item` : 거래의 대상이 되는 물품 객체<br><br>**[Operations]**<br>**<비즈니스 로직>**<br>· `+ Transaction(subjectItem: Item, buyer: User)` : 물품과 수요자 객체를 바탕으로 거래를 생성한다.<br>· `+ approveRequest(): void` / `+ rejectRequest(): void` : 거래 요청을 승인하거나 거절한다.<br>· `+ confirmPickup(): boolean` / `+ confirmReturn(): boolean` : 수령 및 반납 처리를 확정한다.<br>· `+ checkExtensionAvailability(): boolean` : 연장 신청이 가능한 상태인지 검증한다.<br>· `+ getTransactionDetailResponse(): TransactionDetailDTO` : 거래 상세 데이터를 반환한다.<br>· `+ getHistoryByUser(user: User): List<Transaction>` : 특정 유저의 거래 이력을 조회한다.<br>**<Getters (데이터 조회용)>**<br>· `+ getTransactionId(): Long` : 거래 번호를 조회한다.<br>· `+ getStage(): String` : 진행 단계를 조회한다.<br>· `+ getTransactionType(): String` : 거래 종류를 조회한다.<br>· `+ getTransactionDate(): Date` : 시작일을 조회한다.<br>· `+ getReturnDueDate(): DateTime` : 반납 기한을 조회한다.<br>· `+ getBuyer(): User` : 수요자 객체를 조회한다.<br>· `+ getSubjectItem(): Item` : 대상 물품 객체를 조회한다. |
| **Reservation** | 대여 중인 물품에 대한 차기 대기자 예약 정보를 저장하는 클래스이다.<br><br>**[Attributes]**<br>· `- reservationId: Long` : 예약 신청 건의 고유 식별 번호<br>· `- queueOrder: int` : 대상 물품에 대한 현재 대기 순번<br>· `- requestTime: DateTime` : 예약을 신청한 시간<br>· `- status: String` : 예약의 현재 상태 (대기, 취소, 이행 등)<br>· `- targetItem: Item` : 예약 대상 물품 객체<br>· `- borrower: User` : 예약을 신청한 대기자 객체<br><br>**[Operations]**<br>**<비즈니스 로직>**<br>· `+ Reservation(targetItem: Item, borrower: User)` : 물품과 신청자 객체로 예약을 생성한다.<br>· `+ cancelReservation(): void` : 예약 신청을 스스로 취소한다.<br>· `+ processAutoSuccession(): void` : 선행 거래 종료 시 자동으로 대여 권한을 이행시킨다.<br>· `+ getReservationDetailResponse(): ReservationDetailDTO` : 예약 상세 데이터를 반환한다.<br>**<Getters (데이터 조회용)>**<br>· `+ getReservationId(): Long` : 예약 번호를 조회한다.<br>· `+ getQueueOrder(): int` : 대기 순번을 조회한다.<br>· `+ getRequestTime(): DateTime` : 신청 시간을 조회한다.<br>· `+ getStatus(): String` : 예약 상태를 조회한다.<br>· `+ getTargetItem(): Item` : 대상 물품 객체를 조회한다.<br>· `+ getBorrower(): User` : 대기자 객체를 조회한다. |
| **Extension** | 대여 기간 연장 신청 내역을 관리하는 엔티티 클래스이다.<br><br>**[Attributes]**<br>· `- extensionId: Long` : 연장 신청 건의 고유 식별 번호<br>· `- extendDays: int` : 연장을 희망하는 추가 일수<br>· `- reason: String` : 연장을 신청하는 사유<br>· `- status: String` : 연장 신청의 처리 상태 (대기, 승인 등)<br>· `- targetTransaction: Transaction` : 연장 대상이 되는 거래 객체<br><br>**[Operations]**<br>**<비즈니스 로직>**<br>· `+ Extension(targetTransaction: Transaction, extendDays: int, reason: String)` : 거래 객체와 사유를 받아 연장 신청을 생성한다.<br>· `+ approveExtension(): void` / `+ rejectExtension(): void` : 연장 신청을 승인하거나 거절한다.<br>· `+ getExtensionDetailResponse(): ExtensionDetailDTO` : 연장 신청 상세 데이터를 반환한다.<br>**<Getters (데이터 조회용)>**<br>· `+ getExtensionId(): Long` : 연장 번호를 조회한다.<br>· `+ getExtendDays(): int` : 연장 일수를 조회한다.<br>· `+ getReason(): String` : 연장 사유를 조회한다.<br>· `+ getStatus(): String` : 처리 상태를 조회한다.<br>· `+ getTargetTransaction(): Transaction` : 대상 거래 객체를 조회한다. |
| **ChatRoom** | 당사자 간 실시간 소통 허브가 되는 채팅방 클래스이다.<br><br>**[Attributes]**<br>· `- chatRoomId: Long` : 채팅방의 고유 식별 번호<br>· `- topicItem: Item` : 대화의 주제가 되는 대상 물품 객체<br>· `- participants: List<User>` : 채팅방에 참여 중인 당사자 목록<br><br>**[Operations]**<br>**<비즈니스 로직>**<br>· `+ loadChatHistory(): List<ChatMessage>` : 과거 채팅 메시지 내역을 불러온다.<br>· `+ getRoomInfoResponse(): ChatRoomInfoDTO` : 채팅방 메타데이터를 반환한다.<br>**<Getters (데이터 조회용)>**<br>· `+ getChatRoomId(): Long` : 채팅방 번호를 조회한다.<br>· `+ getTopicItem(): Item` : 주제 물품 객체를 조회한다.<br>· `+ getParticipants(): List<User>` : 참여자 목록을 조회한다. |
| **ChatMessage** | 채팅방 내부에서 전송되는 개별 메시지들을 저장하는 클래스이다.<br><br>**[Attributes]**<br>· `- messageId: Long` : 전송된 메시지의 고유 식별 번호<br>· `- content: String` : 전송한 대화 내용 본문<br>· `- sendTime: DateTime` : 메시지가 전송된 시각<br>· `- room: ChatRoom` : 소속된 상위 채팅방 객체<br>· `- sender: User` : 메시지를 전송한 유저 객체<br><br>**[Operations]**<br>**<비즈니스 로직>**<br>· `+ ChatMessage(room: ChatRoom, sender: User, content: String)` : 소속 방, 발신자, 내용을 받아 메시지를 생성한다.<br>· `+ getMessageDetailResponse(): ChatMessageDTO` : 메시지 상세 데이터를 반환한다.<br>**<Getters (데이터 조회용)>**<br>· `+ getMessageId(): Long` : 메시지 번호를 조회한다.<br>· `+ getContent(): String` : 대화 내용을 조회한다.<br>· `+ getSendTime(): DateTime` : 전송 시각을 조회한다.<br>· `+ getRoom(): ChatRoom` : 소속 채팅방 객체를 조회한다.<br>· `+ getSender(): User` : 발신자 객체를 조회한다. |
| **Review** | 거래가 종료된 후 작성되는 서비스 평가 엔티티 클래스이다.<br><br>**[Attributes]**<br>· `- reviewId: Long` : 작성된 리뷰의 고유 식별 번호<br>· `- rating: int` : 거래 만족도를 나타내는 별점<br>· `- content: String` : 작성한 상세 후기 내용<br>· `- targetTransaction: Transaction` : 평가의 바탕이 된 거래 객체<br>· `- reviewer: User` : 리뷰를 작성한 유저 객체<br>· `- targetUser: User` : 평가를 받는 유저 객체<br><br>**[Operations]**<br>**<비즈니스 로직>**<br>· `+ Review(targetTransaction: Transaction, reviewer: User, targetUser: User, rating: int, content: String)` : 대상 및 평점 정보를 바탕으로 리뷰를 생성한다.<br>· `+ getReviewDetailResponse(): ReviewDetailDTO` : 리뷰 상세 데이터를 반환한다.<br>**<Getters (데이터 조회용)>**<br>· `+ getReviewId(): Long` : 리뷰 번호를 조회한다.<br>· `+ getRating(): int` : 별점을 조회한다.<br>· `+ getContent(): String` : 후기 내용을 조회한다.<br>· `+ getTargetTransaction(): Transaction` : 평가 대상 거래 객체를 조회한다.<br>· `+ getReviewer(): User` : 작성자 객체를 조회한다.<br>· `+ getTargetUser(): User` : 피평가자 객체를 조회한다. |
| **Notification** | 시스템의 중요한 상태 변화를 알리는 푸시 메시지 관리 클래스이다.<br><br>**[Attributes]**<br>· `- notificationId: Long` : 푸시 알림의 고유 식별 번호<br>· `- type: String` : 알림의 카테고리 분류 (채팅, 시스템 등)<br>· `- content: String` : 표시될 알림 문구<br>· `- isRead: boolean` : 알림 읽음 여부 확인 플래그<br>· `- receiver: User` : 알림을 수신해야 하는 타겟 유저 객체<br><br>**[Operations]**<br>**<비즈니스 로직>**<br>· `+ sendPushAlert(receiver: User, type: String, content: String): void` : 특정 수신자에게 푸시 알림을 발송한다.<br>· `+ markAsRead(): boolean` : 알림을 읽음 상태로 변경한다.<br>· `+ getNotificationDetailResponse(): NotificationDetailDTO` : 알림 상세 데이터를 반환한다.<br>**<Getters (데이터 조회용)>**<br>· `+ getNotificationId(): Long` : 알림 번호를 조회한다.<br>· `+ getType(): String` : 알림 타입을 조회한다.<br>· `+ getContent(): String` : 알림 문구를 조회한다.<br>· `+ getIsRead(): boolean` : 읽음 여부를 조회한다.<br>· `+ getReceiver(): User` : 수신자 객체를 조회한다. |

## 3.Sequence Diagram

### 1) 회원가입 (Register)
<img width="3260" height="2220" alt="image" src="https://github.com/user-attachments/assets/4c714e44-48a6-43b9-943c-a3efbb797feb" />

시스템의 기능 중 회원가입 기능을 수행할 때를 표현한 Sequence Diagram이다. 미인증 유저(Guest)가 register(userId, password, name)를 호출하여 AuthManager에 등록을 요청한다. AuthManager는 중복되지 않은 아이디인 경우 User 인스턴스를 생성하고 가입 성공을 의미하는 true를 반환한다. 반대로 중복된 아이디인 경우에는 가입을 거부하고 false를 반환하여 가입에 실패하게 된다.

---

### 2) 로그인 (Login)
<img width="2512" height="3576" alt="image" src="https://github.com/user-attachments/assets/c48062cb-ed92-4ceb-8427-6d5e251d0ca5" />

시스템 실행 후 로그인 기능을 수행할 때를 표현한 Sequence Diagram이다. 미인증 유저가 login(userId, password)을 요청하면 AuthManager는 아이디가 존재하는지 먼저 확인한다. 존재하는 유저일 경우 User 객체로부터 getPassword()와 getAccountStatus()를 호출하여 필요한 검증 데이터를 반환받는다. 입력한 비밀번호가 일치하고 계정 상태가 "활성"인 경우에만 로그인 성공(true)을 반환하며, 비밀번호가 틀리거나 정지된 계정, 혹은 존재하지 않는 아이디일 경우 로그인 실패(false)를 반환한다.

---

### 3) 물품 등록 (Item Registration)
<img width="2928" height="2856" alt="image" src="https://github.com/user-attachments/assets/78ee313d-c427-4337-8be8-932862afcb4b" />

물품 등록 과정을 나타낸 Sequence Diagram이다. 물품 소유자(owner)인 User가 자신의 계정 상태를 getAccountStatus()를 통해 검사한다. 계정이 "활성" 상태인 경우에 한해 입력된 정보를 바탕으로 Item 객체를 생성한다. 이후 거래 타입이 "매매"인지 "대여"인지에 따라 분기하여 물품의 초기 상태를 각각 "판매중" 또는 "대여가능"으로 설정한 후 최종 등록을 완료한다.

---

### 4) 검색 및 상세 조회 (Search & Filter)
<img width="2768" height="3220" alt="image" src="https://github.com/user-attachments/assets/231a47ea-30ad-45e1-af7a-5e7902f9e702" />

물품 목록 검색 및 특정 물품의 상세 정보를 조회하는 기능에 대한 Sequence Diagram이다. 수요자(buyer)가 조건부로 searchItem을 호출하여 전체 조건에 맞는 물품 리스트를 반환받는다. 특정 물품을 상세 조회하고자 할 경우 opt 프레임 내에서 해당 Target 물품에 접근하여 제목, 가격, 최대 대여 일수, 그리고 소유자 정보(owner)를 Getter 메서드를 통해 개별적으로 반환받아 화면에 출력한다.

---

### 5) 거래 신청 및 처리 (Transaction)
<img width="3132" height="3596" alt="image" src="https://github.com/user-attachments/assets/09aa4bac-3516-470f-b0f0-c3e5d5fa02c9" />

수요자가 특정 물품에 대한 거래를 신청하고 공급자가 이를 처리하는 과정을 표현한 Sequence Diagram이다. 수요자(buyer)가 대상 물품에 대해 Transaction 객체를 생성하며 신청을 진행하면 시스템은 대기 상태가 된다. 판매자(seller)가 거래를 승인(approveRequest())할 경우 Transaction 객체는 자체 진행 단계를 "승인됨"으로 갱신하고 대상 물품의 상태도 "거래중"으로 연쇄 변경한다. 판매자가 거래를 거절(rejectRequest())할 경우에는 거래 객체의 상태만 "거절됨"으로 변경하며 대상 물품의 상태는 보존한다.

---

### 6) 예약 신청 (Reservation)
<img width="3360" height="4804" alt="image" src="https://github.com/user-attachments/assets/fd422a58-8de0-4dae-ba0a-b5fb3405a04a" />

대여 물품에 대해 차기 대기자가 예약을 신청하는 과정을 표현한 Sequence Diagram이다. 대기자(borrower)가 대상 물품의 상태가 "대여 중"인지 점검한다. 대상 물품이 본인의 물품이 아니고 대기 인원 제한 수치를 초과하지 않았다는 검증 과정을 통과한 경우 예약 신청을 진행할 수 있다. 조건이 성립되면 Reservation 객체를 생성하여 대기 순번을 부여받고 승인 대기 상태로 전환되며, 공급자(owner)가 해당 예약을 최종 승인(approveReservation())하거나 거절(rejectReservation())하여 예약을 처리한다.

---

### 7) 대여 기간 연장 (Extension)
<img width="3644" height="3320" alt="image" src="https://github.com/user-attachments/assets/8ff9225a-15af-4630-8757-495aa31e4fdf" />

물품의 대여 기간 연장 기능을 수행할 때를 표현한 Sequence Diagram이다. 구매자(buyer)가 대여 중인 타겟 거래 객체를 대상으로 연장 가능 여부를 확인(checkExtensionAvailability())한다. 연장이 가능한 상태일 때만 Extension 객체를 생성하여 연장 일수 및 사유와 함께 신청을 접수한다. 판매자(seller)는 접수된 연장 신청을 확인 후 승인(approveExtension())하거나 거절(rejectExtension())하여 처리를 완료한다.

---

### 8) 거래 내역 조회 (Transaction History)
<img width="3284" height="2112" alt="image" src="https://github.com/user-attachments/assets/c68e9416-b7f6-4bcc-aa2c-6a5f479fc4e9" />

유저가 자신의 이전 및 현재 거래 내역을 조회하는 기능의 Sequence Diagram이다. 유저가 본인의 계정을 파라미터로 하여 거래 내역 리스트 조회를 요청하여 목록을 반환받는다. 반환된 리스트에 데이터가 존재할 경우 반복문(loop) 구문을 통해 리스트 내부의 각 거래 객체로부터 상세 정보 데이터(getTransactionDetailResponse())를 개별적으로 호출하여 종합한다.

---

### 9) 수령 확정 및 반납 (Confirm Pickup & Return)
<img width="3992" height="4152" alt="image" src="https://github.com/user-attachments/assets/953ec62e-3d73-4123-9d57-4cf5cff27202" />

계약된 거래의 수령 확정 및 대여 반납 기능을 표현한 Sequence Diagram이다. 시스템은 현재 진행 중인 거래 종류를 먼저 확인한다. 매매 거래일 경우 수령을 확정(confirmPickup())하고 대상 물품의 상태를 "판매 완료"로 변경하여 거래를 정상 종료시킨다. 대여 거래일 경우 반납을 확정(confirmReturn())한 뒤 물품을 다시 "대여 가능" 상태로 복구시킨다. 특히 대여 반납 시점에 해당 물품에 대해 대기 중인 예약자가 존재할 경우 자동으로 대여 권한을 이행시키는 승계(processAutoSuccession()) 프로세스가 실행된다.

---

### 10) 리뷰 작성 (Review)
<img width="3564" height="2268" alt="image" src="https://github.com/user-attachments/assets/1317c298-4598-44b3-8850-cc2c6590b16b" />

거래 종료 후 당사자 간의 서비스 평가를 작성하는 과정을 표현한 Sequence Diagram이다. 구매자(reviewer)가 거래 객체의 진행 단계를 확인하여 "반납 완료"와 같이 거래가 완전히 종료된 시점에서만 평가 로직을 수행한다. Review 객체를 생성함과 동시에 객체지향적 책임 연쇄 로직에 따라 대상 유저(TargetUser)의 매너 점수를 직접 갱신(updateScore())함으로써 리뷰 작성 및 평점 반영 과정을 마무리한다.

---

### 11) 실시간 채팅 (ChatRoom)
<img width="4476" height="3160" alt="image" src="https://github.com/user-attachments/assets/aaf2562e-eb2d-4bd0-a629-18c943f745ad" />

채팅방 내부에서 메시지 전송 및 알림을 수행하는 기능에 대한 Sequence Diagram이다. 발신자(sender)가 메시지 객체를 생성하여 저장한 후 채팅방을 통해 상대 수신자의 접속 여부를 확인한다. 상대방이 미접속 상태일 경우 Notification 클래스를 활용하여 스마트폰에 푸시 알림을 발송한다. 푸시 알림을 받은 수신자가 이를 클릭하면 읽음 처리(markAsRead())가 발생하고 채팅방에 진입하여 과거의 채팅 내역을 로드한다.

---

### 12) 관리자 제재 및 모니터링 (Admin)
<img width="4136" height="5236" alt="image" src="https://github.com/user-attachments/assets/bc2a27b7-10d4-4828-8a31-4c6c6ef674cb" />

관리자가 시스템을 모니터링하고 문제 유저 및 게시글에 대한 제재를 수행하는 과정을 나타낸 Sequence Diagram이다. 관리자(AdminActor)가 전체 시스템 데이터와 의심 유저의 거래 내역을 열람한다. 불량 사용자로 판별되어 기간 정지나 영구 정지 처분을 내릴 때 AdminController가 해당 User 객체에 상태 변경(changeAccountStatus())을 직접 위임하여 상태를 갱신한다. 부적절한 게시글이나 규정을 위반한 물품이 발견된 경우에도 관리자가 해당 물품의 상태를 "삭제됨"으로 갱신하여 시스템 안전을 확보한다.
